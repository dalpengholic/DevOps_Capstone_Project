apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-blue
  labls:
    app: myapp
spec:
  replicas: 2
  selector:
      matchLabels:
          app: myapp
          version: blue
  template:
    metadata:
      labels:
        app: myapp
        version: blue
    spec:
      containers:
        - name: myapp
          image: hub.docker.com/repository/docker/dalpengholic/devops_capstone_green
          imagePullPolicy: Always
          ports:
            - containerPort: 80
    
